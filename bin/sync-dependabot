#!/usr/bin/env bash

set -euo pipefail

clojure -A:defaults -Spom
mkdir -p dependabot
mv pom.xml dependabot

clojure -A:defaults -X:deps list > dependabot/deps-list.txt
clojure -A:defaults -X:deps tree > dependabot/deps-tree.txt
